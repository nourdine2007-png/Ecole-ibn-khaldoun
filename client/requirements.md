## Packages
framer-motion | Smooth animations for page transitions and elements
react-hook-form | Efficient form handling for the contact page
@hookform/resolvers | Zod validation integration for forms
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The entire application layout must be Right-to-Left (RTL) for Arabic.
Google Font 'Cairo' will be used for typography.
Images will use Unsplash placeholders where dynamic data isn't available yet.
